Research ledger

I settled on one question: can a refusal-direction probe predict model behavior
when the prompt is under attack, and can its failures distinguish missed harm
detection from compliance despite detection?

The first pilot used Qwen3-4B and showed strong harmful/harmless separation on
the small hand-written fitting set. That observation is only a pilot. It does
not establish attack-family AUROC or causality.

Important measurement failure: I generated 50 tokens and keyword-labeled the
decoded text. Qwen3 was thinking by default, so those tokens were mostly inside
`<think>`. The apparent compliance counts and ambiguous rows describe the
reasoning trace, not the final answer. I am retaining the pilot notebook output
as an audit trail but excluding every pilot outcome number from the submission.

The submission rerun must:
- toggle thinking explicitly;
- wait for `</think>` and label only the final answer;
- keep truncated rows separate;
- use token IDs directly for the full projection trajectory;
- fit on JailbreakBench/Alpaca prompts disjoint from the battery;
- include no-attack and attack-wrapped-harmless controls;
- use a blinded LLM judge checked against 50 human labels;
- report bootstrap confidence intervals and held-out Youden-J calibration;
- run directional ablation and differential steering before making a causal
  claim.

No attack-family AUROC, steering effect, or final headline result has been
recorded here because the corrected battery has not yet produced those values.
